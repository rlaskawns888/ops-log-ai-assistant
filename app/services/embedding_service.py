def create_embedding(text: str) -> list[float]:
    """
        TODO:
        실제 구현에서는 OpenAI Embedding API 또는 다른 embedding model을 호출한다.
        현재는 전체 서비스 흐름 테스트를 위한 더미 embedding을 반환한다.
    """

    return [0.0] * 1536